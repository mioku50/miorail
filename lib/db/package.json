{
  "name": "@mioagent/db",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "push": "drizzle-kit push",
    "push:test": "drizzle-kit push --force",
    "cleanup:test": "node --import tsx cleanupTestRun.ts",
    "generate": "drizzle-kit generate",
    "test": "NODE_ENV=test MIOAGENT_TEST_SUITE=unit node --import tsx --test index.test.ts testDatabaseGuard.test.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "0.10.4",
    "dotenv": "^16.6.1",
    "drizzle-orm": "^0.36.4",
    "postgres": "^3.4.9"
  },


  "devDependencies": {
    "drizzle-kit": "^0.28.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
