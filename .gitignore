node_modules
dist
.env
.env.*
!.env.example
*.env.backup*
.env.backup*
*.js
!scripts/validate_tasks.js
!scripts/validate_tasks.test.js
# Phase 15.1 — the manual launch check a human pastes into the browser. It is
# source, not build output: the authenticated smoke needs a signed wallet
# session, which no shell here can produce.
!scripts/manual-phase15-auth-smoke.js
!eslint.config.mjs
*.tsbuildinfo
coverage/
playwright-report/
test-results/
mioku.pem

# Agent scaffolding and working files. Kept on disk, out of the repository:
# these are one maintainer's prompts, task queues and throwaway scripts, not
# part of the product — publishing them documents a workflow rather than the
# software.
.agents/
.claude/
scratchpad/
videos/
screen/
agent_tasks.md
verify_ui.py
dummy-plan.txt
frontend-screenshot.png
